plugins {
    id 'org.jetbrains.kotlin.jvm' version "${kotlinVersion}"
}

repositories {
    mavenCentral()
}

subprojects {
    apply plugin: "maven"
    repositories {
        mavenCentral()
    }
}
